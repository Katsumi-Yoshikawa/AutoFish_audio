# AutoFish_audio
AutoFish_audio is a native macOS utility for monitoring loopback/system audio and sending configured keyboard actions to a selected target application. It is designed for workflows where an audible event should trigger a small, repeatable key sequence without manually switching back to the target app (e.g., WoW, MineCraft. etc.).
